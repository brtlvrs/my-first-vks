[env]
# No VCF_CLUSTER here on purpose — a VirtualMachine is a Supervisor-namespace-scoped resource,
# never deployed "into" a VKS guest cluster. Rename this folder (and this value) to match the
# real vSphere Namespace you're deploying into.
VCF_NAMESPACE = "example-namespace"
