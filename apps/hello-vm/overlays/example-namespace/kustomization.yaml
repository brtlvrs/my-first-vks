apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The vSphere Namespace itself — not an app-specific namespace, see base/vm.yaml's comment.
namespace: example-namespace
resources:
  - ../../base
