apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The vSphere Namespace itself — not an app-specific namespace, see components/namespace/'s comment.
namespace: example-namespace
resources:
  - ../../base
components:
  - ../../components/vsphere-pod
