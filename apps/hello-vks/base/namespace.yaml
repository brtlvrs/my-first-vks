apiVersion: v1
kind: Namespace
metadata:
  name: hello-vks
  labels:
    # Restricted Pod Security Standard — the deployment below is written to comply with it
    # (non-root, no privilege escalation, all capabilities dropped). See docs/09.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
