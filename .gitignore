# Per-user/per-customer overrides — never commit real endpoints/credentials
.mise.local.toml
.config/
.old
secret.yaml
*-credentials.txt
.data-store.yaml

# Fetched per-environment, never committed (see docs/06-connecting-to-supervisor.md)
*-ca.pem

# Generated by mise tasks
cluster-kubeconfig.yaml
